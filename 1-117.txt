Ќе забывайте объ€вл€ть локальные переменные!

≈сли вы используете переменную, котора€ не была ранее объ€влена, така€ переменна€ будет глобальной.
Ёто означает, что даже если переменна€ впервые используетс€ внутри функции (потому что вы собирались
сделать ее локальной), эта переменна€ на самом деле будет глобальной и окажетс€ доступной и за пределами
функции (что позднее может создать путаницу). ¬ общем, не забывайте объ€вл€ть локальные переменные!

≈сли вы забудете объ€вить переменную перед использованием, то така€ переменна€ всегда будет глобальной
(даже если она впервые используетс€ внутри функции).

function playTurn(player, location){
	points = 0;
	if (location == 1){
		points = points + 100;
	}
	return points;
}

var total = playTurn("Jai", 1);
alert(points)

ћы забыли объ€вить points с ключевым словом var перед использованием. ѕеременна€ points автоматически 
становитс€ глобальной.

ѕолучаетс€, что переменную points можно будет использовать за пределами функции! ≈е значение не исчезает 
(как должно быть после завершени€ функции)

ѕрограмма ведет себ€ так, словно она была написана в следующем виде:

“ак как мы забыли использовать var, JavaScript считает, что переменна€ points должна быть глобальной,
и ведет себ€ так, словно объ€вление points распологаетс€ на глобальном уровне.

var points = 0;
function playTurn(player, location){
	points = 0;
	if (location == 1){
		points = points + 100;
	}
	return points;
}
var total = playTurn("Jai", 1);
alert(points)

ѕропущенные объ€влени€ локальных переменных могут создать проблемы, если их имена совпадают с именами
других глобальных переменных. ¬ итоге вы можете случайно изменить значение, которое измен€ть не собирались.
