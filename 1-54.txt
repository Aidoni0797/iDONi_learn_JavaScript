Стань браузером. Решение

Ниже приведен код JavaScript, содержащий ошибки. Представьте себя на месте браузера и попробуйте отыскать ошибки
в коде. А когда это будет сделано, загляните в ответы в коонце главы и посмотрите, не упустили ли вы чего-
нибудь. Ниже приведено наше решение.

А.
//Test for jokes
var joke = "JavaScript walked into a bar...'; 
- Строки должны заключаться либо в двойные кавычки ("), либо в одиночные кавычки ('). Смешивать разные кавычки нельзя!
var toldJoke = "false";
- Логические значения не заключаются в кавычки, если только вы не собираетесь работать с ними как со строками;
var $punchline = "Better watch out for those semi-colons."
- Имя переменной может начинаться с $, отя лучше так не делать
var %entage = 20;
- Символ % не может использоваться в именах переменных
var result
- Снова пропущен заверщающий символ ";".

if (toldJoke == true){
	Alert($punchline);
	- Должно быть alert, а не Alert. Язык JavaScript учитывает регистр символов
} else 
- Пропущен открывающая скобка
	alert(joke);
}

В
\\ Movie Night - Комментарий должны начинаться с //, а не с \\
var zip code = 98104; - В именах переменных не может быть пробелов
var joe'sFavoriteMovie = Forbidden Planet; - имена переменных не могут содержать кавычки
- но строка "Forbidden Planet" должна быть заключена в кавычки
var movieTicket$       =     9;

if (movieTicket$ >= 9){
	alert("Too much!");
}else{
	alert("We're going to see "+joeisFavoriteMovie);
- Команда if/else не работает из-за недействительного имени переменной
}