—оздание пустого массива (и добавление элементов)

„тобы завершить работу над кодом, сначала нужно узнать, как создать новый массив и как добавить в него 
элементы. ¬ы уже знаете, как создать массив с набором значений:

var genres = ["80s", "90s", "Electronic", "Folk"];

Ётот список называетс€ литералом массива - мы перечисл€ем, какие значени€ вход€т в массив.

Ќо также можно создать пустой массив без указани€ начлаьных элементов:

var genres = [];

Ќовый массив готов к работе: он не содержит элементов и имеет нулевую длину.

Ёто тоже литерал массива, хот€ он и не содержит данных.

» вы уже знаете, как добавл€ть новые элементы в массив. ƒл€ этого достаточно присвоить значение элементу
с указанием индекса:

var genres = [];
genres[0] = "Rockabilly";
genres[1] = "Ambient";
var size = genres.length;

—оздаетс€ новый элемент массива со строкой Rockabilly.

«десь создаетс€ второй элемент массива, содержащий строку Ambient.

«десь size содержит значение 2 - длину массива.

ѕри добавлении новых элементов необходимо следить за тем, по какому ииндексу размещаетс€ добавл€емое
значение. ¬ противном случае вы создадите разреженный массив, то есть массив с дырами (например,
массив со значени€ми в позици€х 0 и 2, но без значени€ в позиции 1). –азреженный массив - это необ€зательно
плохо, но он требует особого внимани€. ј пока следует сказать, что существует другой способ добавлени€
новых элементов, с которым не нужно беспокоитьс€ об индексе. Ёто метод ush, и вот как он работает:

var genres = [];
genres.push("Rockabilly");
genres.push("Ambient");
var size = genres.length;

—оздает новый элемент по следующему доступному индексу (который равен 0) и присваивает ему значение Rockabilly.

—оздает еще один элемент в следующей свободной позиции (1 в данном случае) и присваевает ему значение Ambient.