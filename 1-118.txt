ј что произойдет, если присвоить локальной переменной такое же им€, как у существующей локальной переменной?

ѕроисходит "замещение" глобальной переменной. ƒопустим, в программе имеетс€ глобальна€ переменна€
beanCounter, и вы объ€влете функцию:

var beanCounter = 10;

function getNumberOfItems(ordertype){
	var beanCounter = 0;
	if (ordertype == "order"){
		//„то-то делаем с beanCounter...
	}
	return beanCounter
}

√лобальна€ и локлаьна€ переменные.

¬се обращени€ к beanCounter внутри функции относ€тс€ к локальной переменной, а не к глобальной.
ћы говорим, что глобальна€ переменна€ "замещаетс€" локальной (то есть она не видна, потому что локальна€
переменна€ ее загораживает).

Ћоклаьна€ и глобальна€ переменные никак не вли€ют друг на друга; если изменить одну, это изменение
никак не отразитс€ на другой. Ёти переменные существуют независимо.